spring:
  kafka:
    bootstrap-servers: kafka:29092
  security:
    oauth2:
      resourceserver:
        jwt:
          # Substitua com o seu domínio do Auth0 (deve terminar com a barra /)
          issuer-uri: https://dev-y3883jpsf8nhsfif.us.auth0.com/
server:
  port: 8080
auth0:
  domain: ${AUTH0_DOMAIN}
  clientId: ${AUTH0_CLIENT_ID}
  clientSecret: ${AUTH0_CLIENT_SECRET}
  audience: ${AUTH0_AUDIENCE}
